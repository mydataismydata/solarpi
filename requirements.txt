# Runtime (Phase 2a — inverter over TCP)
fastapi>=0.110
uvicorn[standard]>=0.29

# mDNS advertisement so the Android app auto-discovers the Pi (_solarpi._tcp). Optional:
# server.py degrades gracefully if it's missing. On a real Pi, an avahi .service file works too.
zeroconf>=0.131

# Phase 2b — battery over BLE (added when we wire up the JBD BMS)
# bleak>=0.21

# Phase 3 — mini-split appliance over Wi-Fi (EG4/Deye, Tuya local v3.3). Optional:
# imported lazily and only touched when SOLAR_APPLIANCE_ENABLED=1, so the dashboard
# runs without it. Install on the Pi when enabling the mini-split: pip install tinytuya
# tinytuya>=1.13

# Dev / test
# pytest>=8.0
#
# Protocol core (solardash/codec.py, inverter.py) and the SQLite store are
# stdlib-only, so the tests run on a bare Python with no installs.
